Операции присваивания и сравнения чисел в Си

x = (y = (z = 5)); z = 5 (L - value)
          ~ 5
     ~ 5
~ 5
2 + 2;   f(x);
~~~~~
  2
x = ( y = 2 ) + ( z = 3 );

Там писать не стоит.

x = ( x = 2 ) + ( x = 3 );
x++ (Возвращяет старое значение x) + ( ++ x);
============================================
ПРОГРАММА
int main(); [
    int x = 256;

x += 256; // 512
x -= 256; // 256
x *= 2;   // 512
x /= 8;   // 64
printf("x = %d\n", x);

return 0;
}

============================================

= (5 > 2)
(int) (int)
    int
0 - ложь 1 - правда

============================================
#include <stdio.h>
#include <stdbool.h>

int main(){
  int x;

  x = (5 > -1); редактирование x = (5u > -1) + (3 <= 4); истинные высказывания операция () (4 == 2+2);
       (2*3 != 2+3) + (2*3 != 2+3);
  printf("x = %d\n", x);

  return 0;
}
